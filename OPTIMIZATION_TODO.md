# 系统优化待办事项

## 1. 阶段0预解析超时问题优化

### 问题描述

在 `/start_review` 接口的阶段0预解析中，系统调用大模型（使用 `DOC_PRE_ANALYSIS_PROMPT`）来识别文档中的所有接口信息。目前存在以下问题：

- **单次调用**：阶段0预解析是一次性把整个文档发给大模型，要求它识别所有接口信息
- **无超时保护**：没有针对大模型调用的超时重试机制
- **输入截断风险**：当前将输入限制在 35000 字符进行硬截断，可能导致后面的接口信息丢失
- **长文档风险**：如果文档包含大量接口（几十个甚至上百个），大模型可能因输出内容过多而超时

### 当前代码位置

- `api/http_test_case_generate.py` 第 797-820 行（阶段0预解析逻辑）
- `DOC_PRE_ANALYSIS_PROMPT` 第 350 行（预解析提示词）

### 当前应对措施

1. **限制输入长度**：将文档内容截断至 35000 字符
2. **增加输出token限制**：`max_tokens=10000`
3. **分接口生成**（阶段2）：虽然预解析是一次性调用，但后续生成测试用例时分成了每个接口单独调用

### 优化方向

| 方案 | 描述 | 复杂度 |
|------|------|--------|
| 超时重试机制 | 添加大模型调用超时自动重试逻辑 | 低 |
| 分页预解析 | 将文档分成多个部分多次调用大模型，最后合并结果 | 中 |
| 动态截断 | 根据预估接口数量动态调整截断策略 | 中 |
| 流式输出 | 使用支持流式输出的模型，逐步获取结果 | 高 |

### 优化优先级

- **低优先级**：当前系统对于一般规模文档（10-20个接口）运行稳定
- **高优先级**：当文档接口数量超过 20 个，或文档内容特别长时，可能出现超时问题

---

## 2. 其他待优化项

（后续可根据需要添加更多优化项）

